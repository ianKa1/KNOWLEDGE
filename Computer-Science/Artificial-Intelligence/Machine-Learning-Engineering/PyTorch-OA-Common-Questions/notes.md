## numpy
import numpy as np

np.array()
np.mean()
np.sum()

[f(x) for x in arr_x]

torch.tensor([])
torch.zeros/ones/full/rand
torch.zeros_like/ones_like/full_like/rand_like
torch.linspace(a,b,samples)
torch.from_numpy()

x.unsqueeze()
x = x[:, None, :]
x.view() x.reshape()

torcht.cat([a, b], dim)

matmul @

transpose permute only change stride