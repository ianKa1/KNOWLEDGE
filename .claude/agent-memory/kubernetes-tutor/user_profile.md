---
name: user_profile
description: User's background, knowledge level, and learning context for Kubernetes/Docker study
type: user
---

User is studying containers and Kubernetes as part of a personal CS knowledge base (D:\KNOWLEDGE).
They have existing notes on Docker (core concepts, Dockerfile, commands, ML-specific usage) and are
progressing from Docker fundamentals toward Kubernetes.

Background signals:
- Has ML/AI context (NVIDIA CUDA Dockerfile in notes, GPU usage) — likely a developer or ML engineer
- Notes are well-structured, suggests comfort with technical writing
- Currently at: Docker internals (OS-level understanding) — next logical step is Kubernetes architecture

Knowledge already covered (in notes):
- Containers vs VMs comparison
- Images, layers, Dockerfile, registry, volumes
- Basic Docker commands
- Docker for ML (NVIDIA toolkit, multi-stage builds, Compose)
- High-level Docker → Kubernetes relationship
